# 提交前修改记录

本文件用于记录每次准备提交到 GitHub 前的本地修改。每条记录应说明：修改目的、涉及文件、验证情况，以及是否已经提交或推送。

## 2026-08-13 20:20 - 中文文档与订阅健壮性改进，已提交并推送

### 目的

- 为代码补充中文阅读注释和文件大纲，便于理解设计和调用过程。
- 同时保留英文 README，新增中文 README。
- 提升观察者订阅接口的基础健壮性，并明确观察者生命周期约束。

### 修改内容

- 新增 `README-CN.md`：中文项目说明，包括架构、调用链、滤波策略、设计模式、资源受限 MCU 落地方式。
- 新增 `文件大纲.txt`：按阅读顺序说明每个文件职责和一次采样的调用流程。
- 为 `platform`、`sensor`、`application` 和 `main.cpp` 补充中文注释，说明函数作用、参数、输出和关键变量。
- 修改 `SensorService::subscribe()`：订阅前检查观察者地址，避免同一个观察者被重复订阅并重复回调。
- 在 `README.md`、`README-CN.md` 及代码注释中明确：`SensorService` 不拥有观察者对象；观察者必须在订阅期间保持有效，并在销毁前调用 `unsubscribe()`。

### 涉及文件

- `README.md`
- `README-CN.md`
- `文件大纲.txt`
- `include/application/observers.hpp`
- `include/application/sensor_service.hpp`
- `include/platform/mock_temperature_driver.hpp`
- `include/platform/temperature_driver.hpp`
- `include/sensor/filter.hpp`
- `include/sensor/temperature_sensor.hpp`
- `src/main.cpp`

### 验证

- 已执行 `git diff --check`，未发现空白字符错误。
- 本机未发现可用 C++17 编译器，因此尚未执行 CMake 编译和单元测试。

### Git 状态

- 本地修改：已提交，提交概要为 `docs: add Chinese project guide and clarify observer ownership`。
- GitHub 远端：已推送至 `main` 分支。

## 2026-08-13 20:49 - STM32F103C8T6、DS18B20 与滤波算法扩展，已提交并推送

### 目的

- 为现有跨 MCU 框架补充 STM32F103C8T6 平台下 DS18B20 的实际温度采集实现。
- 保持底层硬件实现与 `ITemperatureDriver` 接口一致，使现有传感器、滤波和应用订阅者无需修改。
- 在既有 `IFilter` 策略接口下补充两种常用温度信号滤波算法。

### 修改内容

- 新增 `embedded/stm32f1/stm32f1_onewire.hpp/.cpp`：基于 STM32 HAL 的 1-Wire 总线时序实现，包含复位、存在脉冲、位和字节读写。
- 新增 `embedded/stm32f1/stm32f1_ds18b20_driver.hpp/.cpp`：实现 `Stm32f1Ds18b20Driver`，完成 DS18B20 ROM 读取、家族码 `0x28` 校验、ROM CRC、scratchpad CRC、温度转换和摄氏度换算。
- 新增 `embedded/stm32f1/README.md`：记录 DS18B20 的 4.7 kOhm 外部上拉、PA1 示例接线、DWT 微秒计时启用方式和集成限制。
- `include/sensor/filter.hpp` 新增 `LowPassFilter`：使用一阶递推公式和 `alpha` 参数，首次采样直接初始化。
- `include/sensor/filter.hpp` 新增 `KalmanFilter`：实现一维标量卡尔曼的预测与更新，支持 Q/R 参数运行时调整。
- 两种新滤波器都实现 `process/reset/name`，不使用动态内存，可直接接入 `SensorService`。
- `src/main.cpp` 增加运行时切换到低通和卡尔曼滤波的演示。
- `tests/framework_tests.cpp` 增加低通初始化/参数钳位和卡尔曼估计范围测试。
- 更新中英文 README 与文件大纲，增加 STM32F1 平台入口及新滤波算法说明。

### 设计边界与限制

- 当前实现仅支持单个、外部 3.3 V 供电的 DS18B20，使用 `SKIP_ROM`；不支持寄生供电和多设备 ROM 搜索。
- `read_raw()` 在 12 位分辨率时最多等待 750 ms，只能在主循环或 RTOS 任务调用，不能在中断中调用。
- 微秒时序使用 Cortex-M3 的 DWT 周期计数器；用户工程必须在启动时启用 `DWT->CYCCNT`。
- STM32 HAL 代码没有加入根目录 PC CMake 目标，因此不会影响 mock 演示构建。

### 验证

- 已执行 `git diff --check`，未发现空白字符错误。
- 当前机器没有 STM32CubeF1 HAL 工程、交叉编译器和实物 DS18B20，尚未完成 STM32 编译、波形和实机验证。
- 当前机器没有可用 C++17 编译器，尚未执行根目录 CMake 编译和单元测试。

### Git 状态

- 本地修改：已提交，提交概要为 `feat: add STM32F1 DS18B20 driver and advanced filters`。
- GitHub 远端：已推送至 `main` 分支。
